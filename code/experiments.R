experiments <- function(){


Group <- c("Control", "Group 1", "Group 2", "Group 3")
Treatment <- c("No lottery", "Individual is entered into a lottery once they are vaccinated", "Everyone is entered into a lottery and they are elgible to win if vaccinated", "Individual is entered if they referred a friend to get vaccinated and friend gets vaccinated")
frame <- data.frame(Group, Treatment)
frame

}